import styled from 'styled-components';

const Container = styled.div`
  align-items: center;
  display: flex;
  padding: 10px;
  cursor: pointer;
  border-radius: 10px;
  margin: 4px;

  &:hover {
    background: rgba(0, 55, 88, 0.08);
  }
`;

export default Container;
