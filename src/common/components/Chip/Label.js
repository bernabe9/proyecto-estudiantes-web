import styled from 'styled-components';

const Label = styled.span`
  display: flex;
  align-items: center;
  user-select: none;
  white-space: nowrap;
  padding-left: 12px;
  padding-right: 12px;
`;

export default Label;
