import styled from 'styled-components';

const BoxCategoryTitle = styled.p`
  color: rgba(0,0,0,0.8);
  text-transform: capitalize;
  text-align: center;
  margin: 0 0 15px 0;
  width: 100%;
  padding-bottom: 5px;
  border-bottom: dotted 1px rgba(0,0,0,0.8);
`;

export default BoxCategoryTitle;
